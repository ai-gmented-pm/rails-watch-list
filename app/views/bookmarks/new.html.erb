<h1>Add a movie to the list <%= @list.name %></h1>
<br>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all, prompt: "Choose a movie" %>
  <%= f.submit %>
<% end %>
